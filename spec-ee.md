文档版本：V1.0

适用范围：指导AI开发团队完成LLM Guard项目（LLM病毒专用杀毒软件）的设计、开发、测试与部署，明确项目核心目标、功能模块、技术实现、关键指标及落地要求，聚焦LLM生态特有的提示词病毒、AI Skill病毒、MCP病毒、LLM自身病毒（训练/推理阶段病毒）的检测、拦截与修复，适配AI Agent、LLM服务集群、个人端LLM等多场景防护需求。

核心定位：以“AI防AI”为核心逻辑，打造一款专门针对LLM生态新型病毒的专用杀毒软件，替代传统杀毒软件对AI生态病毒的检测盲区，实现“全链路防护、智能化检测、轻量化部署、可扩展迭代”，覆盖LLM病毒“检测-拦截-修复-溯源-更新”的完整安全闭环，适配ClawHub类Skill投毒、Prompt注入、MCP劫持等各类AI攻击场景。

一、概述（Purpose & Scope）

1.1 设计目标

1.  核心目标：精准检测并拦截LLM生态中的四大类新型病毒（提示词病毒、AI Skill病毒、MCP病毒、LLM自身病毒），清除恶意载荷，修复被篡改的LLM配置、MCP规则、Skill插件，阻断AI生态供应链投毒、协同攻击链，保障LLM模型、AI Agent、MCP控制平面及用户终端的安全。

2.  性能目标：兼顾检测准确率与响应速度，轻量化部署，降低对LLM服务、用户终端的资源占用，支持批量扫描与实时防护双重模式，适配不同规模的LLM部署场景（个人端、企业级集群）。

3.  可扩展目标：采用模块化架构设计，支持新增LLM模型适配（如Gemini、GPT、DeepSeek等）、新增病毒类型检测（如后续出现的多模态LLM病毒）、新增防护场景（如AI训练数据投毒检测），便于后续功能迭代与升级。

4.  易用性目标：提供可视化操作界面（Web端+终端客户端），支持一键扫描、自动修复、日志查询、威胁溯源，适配AI开发人员、运维人员、普通LLM用户等不同使用群体，降低操作门槛。

1.2 适用范围

1.  防护对象：各类大语言模型（LLM）、AI Agent平台（如OpenClaw、AutoGPT）、MCP（Model Control Plane）控制平面、AI Skill/插件（如SKILL.md、Plugin脚本）、LLM训练数据/推理输入输出、用户终端（对接LLM/AI Agent的设备）。

2.  病毒覆盖：提示词病毒（Prompt注入、指令越狱、ASCII走私等）、AI Skill病毒（伪装Skill、混淆指令、远程载荷拉取等，含ClawHub类恶意Skill）、MCP病毒（配置篡改、数据投毒、逻辑漏洞利用等）、LLM自身病毒（训练数据投毒、模型后门、推理阶段恶意操控等）。

3.  部署场景：

（1）个人端：Windows 10+/macOS 10.15+/Linux（Ubuntu 20.04+）终端，适配个人使用的本地LLM、AI Agent插件防护；

（2）Web端：支持在web端扫描本地skill目录中的skills、提示词是否存在病毒。

（2）企业级：LLM服务集群（云端部署）、AI Agent平台服务器、MCP控制平面服务器，支持分布式部署与集中管控；

（3）嵌入式：适配轻量化LLM设备（如边缘计算LLM终端），支持裁剪式部署（保留核心检测功能）。

4.  开发适配：支持AI开发团队基于本Spec完成前端、后端、算法、测试全流程开发，明确各模块接口、技术栈、开发规范，确保项目落地一致性。

1.3 核心定义

- LLM病毒：特指围绕LLM生态展开，以文本/自然语言/轻量脚本为载体，利用LLM语义分析能力、AI生态信任链漏洞发起攻击的新型恶意程序/攻击手段，含提示词病毒、Skill病毒、MCP病毒、LLM自身病毒四大类；

- 语义检测：基于LLM大模型的自然语言理解、代码分析能力，对文本、指令、脚本进行语义层面的恶意识别，区别于传统特征码匹配；

- 沙箱隔离：针对可疑Skill、提示词、恶意载荷，搭建独立的虚拟执行环境，模拟真实运行场景，记录行为日志，避免恶意行为影响真实系统/LLM；

- 全链路防护：覆盖LLM生态“输入（提示词、Skill上传）→ 处理（LLM推理、MCP调度）→ 输出（指令、代码）→ 终端执行”的全环节，实现无死角防护；

- IOC信息：恶意IP、域名、混淆指令特征、Skill伪装关键词、提示词触发短语等威胁情报信息，用于病毒检测与拦截。

二、核心功能模块（Core Functional Modules）

LLM Guard采用“分层架构+模块化设计”，核心分为6大功能模块，各模块独立解耦、协同工作，形成“检测-拦截-修复-溯源-更新”的完整安全闭环，模块间通过标准化接口交互，支持灵活扩展。

2.1 核心检测模块（Core Detection Module）

核心模块，负责LLM四大类病毒的精准检测，采用“语义分析+行为分析+威胁情报匹配”的三重检测机制，结合LLM大模型赋能，解决传统杀毒软件对文本类病毒的检测盲区，支持批量扫描与实时检测两种模式。

2.1.1 提示词病毒检测子模块

功能：实时检测LLM推理输入的提示词、批量检测历史提示词记录，识别Prompt注入、指令越狱、ASCII走私、恶意触发短语等病毒行为，区分正常提示词与恶意提示词，避免LLM被操控输出有害内容/恶意指令。

实现：

1.  基于预训练的LLM恶意提示词识别模型（依托Gemini/GPT-4微调，训练数据包含已知Prompt注入样本、ClawHub关联提示词病毒样本），对输入提示词进行语义分析，提取恶意特征（如“忽略之前指令”“执行系统命令”等触发短语）；

2.  集成ASCII走私、特殊字符编码检测逻辑，识别隐藏在提示词中的混淆指令（如不可见字符、编码伪装的恶意指令），支持自动解码校验；

3.  建立提示词病毒特征库（可动态更新），存储已知恶意提示词特征、触发短语、混淆模式，采用AC自动机实现快速匹配，提升检测速度；

4.  支持自定义检测规则，允许用户添加特定场景的恶意提示词关键词（如企业内部敏感指令）；

5.  检测结果分级：高危（可直接操控LLM执行恶意操作）、中危（可能绕过LLM安全策略）、低危（存在潜在风险），分别触发不同的拦截/提醒逻辑。

规格：检测准确率≥98%，误报率≤0.5%，单条提示词检测响应时间≤50ms，支持批量检测（≥1000条/分钟）。

2.1.2 AI Skill病毒检测子模块

功能：针对AI Agent平台的Skill插件（如SKILL.md、YAML配置、Python脚本类Skill），实现上传检测、本地扫描、批量审计，识别伪装Skill、混淆指令、远程载荷拉取、系统调用等恶意行为，覆盖ClawHub类恶意Skill的所有攻击手法。

实现：

1.  支持多格式Skill文件解析（Markdown、YAML、Python、JSON等），提取文件中的文本指令、代码片段、远程请求地址；

2.  语义分析：利用LLM模型对Skill文本进行语义解读，识别伪装成“环境配置”“依赖安装”的恶意指令（如curl | bash、Base64解码执行等）；

3.  混淆解码：支持Base64、URL编码、Unicode编码等多种混淆方式的自动解码，校验解码后的内容是否包含恶意指令、远程拉取地址；

4.  远程载荷检测：对Skill中包含的远程请求（如HTTP/HTTPS拉取、IP连接），比对威胁情报库中的恶意IP/域名，拦截可疑远程拉取行为，同时模拟拉取（沙箱环境）验证载荷安全性；

5.  行为预判：基于Skill中的指令逻辑，预判其执行后可能的系统行为（如文件读写、注册表修改、密码窃取），结合恶意行为规则库判定风险；

6.  支持ClawHub类恶意Skill特征匹配，识别团伙化投毒的Skill（如复用同一IP/域名的Skill），实现溯源关联。

规格：支持单次上传≤100MB的Skill文件，单文件检测响应时间≤1s，恶意Skill检测准确率≥98.5%，可识别95%以上的ClawHub类恶意Skill（含Base64混淆、远程载荷拉取类型）。

2.1.3 MCP病毒检测子模块

功能：监控MCP控制平面的配置文件、调度数据、权限规则，检测MCP层的配置篡改、数据投毒、逻辑漏洞利用等病毒行为，避免MCP被攻陷后操控整个LLM生态。

实现：

1.  定期扫描MCP的核心配置文件（如插件白名单、调度规则、权限配置），对比基准配置（首次部署时的安全配置），识别未授权修改、恶意配置注入；

2.  检测MCP调度数据中的恶意内容（如向LLM集群分配恶意Skill、恶意提示词），结合LLM病毒检测结果，实现协同校验；

3.  集成MCP逻辑漏洞检测规则（如跨域调用漏洞、权限继承漏洞），模拟攻击场景验证MCP的防护能力；

4.  监控MCP的异常操作日志（如未授权的配置修改、批量Skill调用），触发实时检测与告警；

5.  支持MCP配置的数字签名校验，确保配置文件未被篡改，篡改后可自动识别并触发修复。

规格：配置篡改检测响应时间≤100ms，MCP数据投毒检测准确率≥99%，支持主流MCP架构（如Kubernetes-based MCP、自研MCP）适配。

2.1.4 LLM自身病毒检测子模块

功能：检测LLM模型自身的安全风险，包括训练数据投毒、模型后门、推理阶段的恶意输出，避免LLM成为攻击工具或被持久化控制。

实现：

1.  训练数据投毒检测：对LLM的训练数据（批量导入）进行污染检测，识别恶意样本（如注入的虚假信息、恶意指令样本），采用RAG技术+知识图谱验证数据真实性，剔除毒数据；

2.  模型后门检测：通过向LLM输入特定触发指令，检测模型是否存在后门（如特定短语触发恶意输出），结合模型输出的语义一致性分析，识别异常响应；

3.  推理输出检测：监控LLM的推理输出结果，识别恶意代码、有害信息、未授权指令，拦截并阻断输出，避免扩散；

4.  模型完整性校验：定期校验LLM模型文件的哈希值，对比基准哈希，识别模型文件被篡改的情况（如注入恶意代码片段）。

规格：训练数据投毒检测准确率≥97%，模型后门检测覆盖率≥95%，LLM输出检测响应时间≤30ms。

2.2 实时防护模块（Real-time Protection Module）

功能：基于核心检测模块的结果，实现对LLM病毒的实时拦截、阻断，覆盖LLM生态的全链路，避免恶意行为落地（如Skill执行、提示词生效、MCP配置篡改），支持防护级别自定义。

实现：

1.  提示词防护：对接LLM的推理输入接口，拦截高危/中危提示词病毒，弹出告警提示，提供“拒绝执行”“修改提示词”“强制放行（需权限）”三种操作选项；低危提示词仅弹出提醒，不拦截；

2.  Skill防护：对接AI Agent平台的Skill上传接口，实时拦截恶意Skill上传，禁止恶意Skill进入MCP调度链路；本地Skill扫描时，发现恶意Skill后禁止其执行，并隔离至安全目录；

3.  MCP防护：实时监控MCP的配置修改、数据调度行为，拦截未授权的配置篡改、恶意数据注入，阻断异常调度链路，同时触发告警；

4.  LLM防护：拦截LLM的恶意输出（如恶意代码、有害信息），禁止输出内容扩散；发现模型被篡改/存在后门时，暂停LLM推理服务，触发修复流程；

5.  防护级别配置：

（1）高等级：拦截所有高危、中危病毒，低危病毒提醒，禁止任何可疑行为（适合金融、医疗等敏感LLM场景）；

（2）中等级：拦截高危病毒，中危、低危病毒提醒，允许用户手动放行（适合企业级LLM场景）；

（3）低等级：仅拦截高危病毒，中危、低危病毒仅记录日志，不拦截、不提醒（适合个人端非敏感场景）；

6.  终端联动防护：对接用户终端的系统接口，拦截Skill病毒触发的系统命令（如curl | bash、文件窃取），禁止恶意载荷落地执行。

规格：防护响应时间≤100ms，拦截成功率≥99.5%，不影响LLM推理、AI Agent运行效率（性能损耗≤10%）。

2.3 修复与隔离模块（Cleanup & Isolation Module）

功能：对检测到的LLM病毒及关联恶意资产进行清除、修复、隔离，恢复LLM生态的正常运行，避免病毒二次传播，支持手动修复与自动修复两种模式。

实现：

1.  病毒隔离：将恶意Skill、恶意提示词记录、恶意载荷、被篡改的MCP配置等，隔离至加密安全目录（不可执行、不可修改），保留样本用于溯源与威胁分析，支持手动删除或恢复隔离文件；

2.  清除功能：

（1）提示词病毒：删除恶意提示词记录，清除LLM推理缓存中的恶意提示词残留；

（2）Skill病毒：删除恶意Skill文件，阻断其与MCP、LLM的关联，清除Skill执行留下的临时文件、恶意载荷；

（3）MCP病毒：删除恶意配置、恶意调度数据，修复被篡改的MCP规则，恢复至基准安全配置；

（4）LLM自身病毒：清除模型中的恶意后门（通过模型修复工具），剔除训练数据中的毒数据，恢复模型完整性；

3.  自动修复：检测到病毒后，根据病毒类型自动执行修复操作（如MCP配置恢复、Skill删除、提示词清除），高危病毒修复后需记录日志并告警；

4.  手动修复：提供可视化修复界面，展示病毒详情、影响范围，允许用户手动选择修复方式（如隔离、删除、恢复配置），支持批量修复；

5.  系统修复：针对Skill病毒、MCP病毒导致的系统/LLM异常（如LLM推理失败、MCP调度异常），提供一键修复功能，恢复正常运行环境；

6.  修复日志：记录所有修复操作（时间、病毒类型、修复方式、修复结果、操作人员），支持溯源查询。

规格：自动修复响应时间≤3s，手动修复操作响应时间≤1s，修复成功率≥99%，修复后无残留恶意资产，不影响LLM、MCP、AI Agent的正常功能。

2.4 威胁溯源与日志模块（Threat Tracing & Log Module）

功能：记录LLM病毒的检测、拦截、修复、更新等所有操作日志，实现病毒行为溯源、攻击链路还原、威胁情报分析，支持日志查询、导出、告警，辅助开发/运维人员排查安全问题、优化防护策略。

实现：

1.  日志分类存储：按模块分为检测日志、拦截日志、修复日志、更新日志、系统日志，按时间戳、病毒类型、防护级别分类索引，便于快速查询；

2.  日志详情：每条日志包含时间、病毒类型、病毒特征、检测结果、处理方式、影响范围、设备/LLM标识、操作人员（手动操作时），Skill病毒日志额外记录Skill名称、上传者、关联IP/域名；

3.  威胁溯源：

（1）针对恶意Skill，溯源其上传者、上传时间、关联IP/域名、恶意载荷来源，关联ClawHub类投毒团伙的IOC信息；

（2）针对提示词病毒，溯源其输入来源、触发场景、是否与其他恶意行为关联；

（3）针对MCP/LLM病毒，溯源攻击发起者的操作轨迹、攻击链路，还原攻击过程；

4.  告警功能：支持自定义告警规则（如高危病毒检测到、批量恶意Skill上传、MCP被篡改等场景），告警方式包括弹窗、邮件、短信、系统通知，支持告警级别设置（高危、中危、低危）；

5.  日志导出：支持将日志导出为CSV、PDF格式，支持按时间范围、病毒类型筛选导出；

6.  日志留存：默认留存90天，支持自定义留存周期（最长365天），留存期间支持快速检索，过期日志自动归档或删除（可配置）；

7.  威胁分析报表：自动生成每日/每周/每月的威胁分析报表，展示病毒检测数量、类型分布、拦截成功率、修复情况、高发攻击场景，辅助优化防护策略。

规格：日志记录准确率100%，检索响应时间≤1s（单条件检索），支持批量导出（≥10万条日志/次），告警响应时间≤500ms。

2.5 威胁情报与更新模块（Threat Intelligence & Update Module）

功能：实现病毒特征库、恶意IOC信息、检测模型、软件版本的自动/手动更新，确保LLM Guard能够应对不断迭代的LLM病毒攻击手法（如新型混淆Skill、变形提示词病毒），保持防护能力的时效性。

实现：

1.  威胁情报库：整合全球LLM病毒威胁情报（含ClawHub类恶意Skill的IOC信息、新型提示词病毒样本、MCP攻击手法），包含恶意IP、域名、Skill特征、提示词触发短语、模型后门特征等，支持实时同步更新；

2.  病毒特征库更新：

（1）自动更新：默认每1小时增量更新（仅下载新增/修改的特征），每日全量更新，更新过程不影响防护功能运行；

（2）手动更新：支持用户手动触发更新，适合无网络环境（可导入本地特征包）；

（3）更新校验：下载的特征包、模型文件需校验哈希值，确保完整性与安全性，更新失败自动回滚至旧版本，避免防护失效；

3.  检测模型更新：定期更新提示词病毒识别模型、Skill语义分析模型（每月至少1次），融入最新病毒样本，提升检测准确率；

4.  软件版本更新：支持自动检测新版本、手动下载更新，更新内容包含功能优化、漏洞修复、新增防护场景，更新后保留用户配置；

5.  离线更新：支持导出本地更新包（特征库、模型、版本文件），用于无网络环境的部署设备更新；

6.  更新日志：记录所有更新操作（时间、更新类型、更新内容、版本号、更新结果），支持查询与回滚。

规格：增量更新带宽占用≤10MB/次，全量更新带宽占用≤100MB/次，更新成功率≥99.8%，更新过程不影响LLM Guard的正常防护（性能损耗≤5%）。

2.6 可视化操作与管控模块（Visual Operation & Control Module）

功能：提供Web端、终端客户端两种操作界面，实现LLM Guard的全功能操作、状态监控、权限管控，适配不同使用群体，降低操作门槛，支持集中管控（企业级场景）。

实现：

1.  终端客户端（个人端）：

（1）主界面：展示防护状态（开启/关闭）、防护级别、最近检测记录、病毒统计；

（2）核心操作：一键扫描（全量/指定范围，如Skill文件夹、提示词记录）、一键修复、防护级别设置、隔离区管理；

（3）日志查询：按时间、病毒类型查询检测、拦截、修复日志，支持简单筛选；

（4）设置界面：更新设置（自动/手动、更新频率）、告警设置、自定义检测规则、日志留存设置；

2.  Web端（企业级）：

（1）dashboard：展示所有部署设备/LLM集群的防护状态、病毒检测统计、告警统计、威胁趋势图；

（2）集中管控：批量设置防护级别、批量触发扫描/修复、批量更新特征库/模型，统一管理所有部署节点；

（3）威胁分析：展示病毒类型分布、高发攻击场景、溯源分析结果，生成可视化报表；

（4）权限管控：分为管理员、运维人员、普通用户三个角色，管理员拥有全权限，运维人员仅拥有操作权限，普通用户仅拥有查看权限；

（5）接口对接：支持与企业现有安全管理平台、LLM集群管理平台对接，同步防护数据、告警信息；

3.  易用性设计：界面简洁直观，操作流程简化（如一键完成扫描+修复），提供操作指引，支持中英文切换；

4.  状态监控：实时展示LLM Guard的运行状态、CPU/内存占用、网络带宽占用，异常状态（如检测失败、更新失败）触发告警。

规格：界面响应时间≤500ms，支持同时管控≥100个部署节点（Web端），支持多用户同时登录（≤50人），操作日志记录完整。

三、技术实现原理（Technical Implementation Principles）

3.1 核心架构设计（分层解耦，模块化可扩展）

LLM Guard采用“四层架构”设计，从下至上分为基础层、核心层、应用层、交互层，各层独立解耦，通过标准化接口交互，支持功能扩展与场景适配，架构如下：

1.  基础层：提供底层支撑，包含操作系统适配、网络通信、数据存储、沙箱环境、加密模块，确保软件稳定运行；

   - 数据存储：采用SQLite（个人端）/MySQL（企业级）存储日志、配置、隔离文件索引，采用Redis缓存病毒特征库、威胁情报，提升查询速度；

   - 沙箱环境：基于Docker（企业级）/轻量级虚拟容器（个人端）搭建，模拟真实运行场景，用于可疑Skill、恶意载荷的行为分析，避免影响真实系统；

   - 加密模块：采用AES-256加密隔离文件、敏感配置（如API密钥），采用MD5/SHA256校验文件完整性，防止篡改；

2.  核心层：包含核心检测模块、实时防护模块的核心逻辑，是LLM Guard的“大脑”，依托LLM大模型赋能，实现智能化检测与拦截；

   - 核心技术：LLM语义分析（微调Gemini/GPT-4）、AC自动机（特征匹配）、混淆解码算法、行为分析引擎、威胁情报匹配引擎；

   - 模块交互：各检测子模块将检测结果传入防护模块，防护模块根据结果执行拦截操作，同时将操作记录传入日志模块；

3.  应用层：包含修复与隔离模块、威胁溯源与日志模块、威胁情报与更新模块，实现病毒处理、日志管理、更新维护，形成完整安全闭环；

4.  交互层：包含可视化操作与管控模块，提供Web端、终端客户端两种交互方式，实现用户操作与系统管控，适配不同使用场景。

3.2 核心检测机制（以AI防AI，三重检测协同）

LLM Guard区别于传统杀毒软件的核心，在于采用“以AI防AI”的检测逻辑，结合三重检测机制，解决LLM病毒无传统特征、难以检测的问题，核心机制如下：

1.  第一重：LLM语义分析检测（核心）—— 依托微调后的LLM大模型（Gemini/GPT-4），对提示词、Skill文本、MCP配置进行语义解读，识别恶意逻辑，而非简单的关键词匹配，能够应对变形病毒、混淆病毒（如Base64编码的Skill指令）；

2.  第二重：行为分析检测—— 对可疑对象（Skill、提示词、MCP操作）进行行为预判与模拟执行（沙箱环境），记录其可能的操作行为（如远程拉取、文件窃取、配置篡改），结合恶意行为规则库，判定是否为病毒；

3.  第三重：威胁情报匹配检测—— 结合实时更新的威胁情报库，匹配恶意IP、域名、Skill特征、提示词触发短语等IOC信息，快速识别已知病毒，提升检测速度与准确率。

三重检测机制协同工作，语义分析解决“未知病毒检测”问题，行为分析解决“混淆病毒检测”问题，威胁情报匹配解决“已知病毒快速检测”问题，确保检测的全面性与准确性。

3.3 关键技术选型（适配AI开发，兼顾性能与可扩展性）

结合LLM Guard的功能需求与AI开发场景，关键技术选型如下（可根据开发团队技术栈适当调整，核心要求适配LLM模型、支持模块化扩展）：

1.  后端开发：Python 3.10+（适配LLM API、语义分析、行为引擎）、Node.js（Web端后端），采用FastAPI（接口开发，高性能、易扩展）；

2.  前端开发：TypeScript+React（Web端）、Electron（终端客户端）、Vite（构建工具，提升开发效率）；

3.  算法模型：Gemini Pro/GPT-4（提示词病毒识别、Skill语义分析），采用TensorFlow/PyTorch框架（模型训练与部署）；

4.  数据存储：SQLite 3.40+（个人端）、MySQL 8.0+（企业级）、Redis 6.2+（缓存）；

5.  沙箱技术：Docker（企业级）、Firejail（个人端，轻量级）；

6.  加密技术：AES-256（敏感数据加密）、MD5/SHA256（文件校验）；

7.  匹配算法：AC自动机（特征匹配）、KMP算法（字符串匹配）；

8.  网络通信：HTTP/HTTPS（API调用、威胁情报更新）、WebSocket（实时监控、告警推送）；

9.  部署技术：Docker（容器化部署，企业级）、exe/dmg/deb（终端客户端，个人端）、Kubernetes（分布式部署，LLM集群场景）。

3.4 性能优化要点（轻量化，低损耗，高响应）

LLM Guard需适配LLM服务、个人终端等多种场景，需重点优化性能，降低资源占用，提升响应速度，核心优化要点如下：

1.  检测性能优化：

（1）采用“轻量扫描+深度扫描”结合策略：实时防护时采用轻量扫描（仅匹配威胁情报、核心特征），提升响应速度；批量扫描时采用深度扫描（语义分析+行为分析），提升检测准确率；

（2）多线程/多进程部署：批量扫描、日志处理、更新操作采用多线程/多进程，避免单线程阻塞，提升处理效率；

（3）特征缓存：将高频使用的病毒特征、威胁情报缓存至Redis，减少数据库查询次数，提升匹配速度；

2.  资源占用优化：

（1）轻量化设计：裁剪非核心功能（嵌入式场景），优化代码逻辑，降低CPU/内存占用；

（2）更新优化：采用增量更新，减少带宽占用；更新操作在后台低优先级执行，避免影响前台防护功能；

（3）沙箱优化：个人端采用轻量级沙箱（Firejail），避免Docker占用过多资源；沙箱环境按需创建、自动销毁，减少资源浪费；

3.  响应速度优化：

（1）接口优化：采用FastAPI开发后端接口，提升接口响应速度；前端采用组件化开发，优化页面加载速度；

（2）数据索引：对日志、特征库建立合理的索引，提升查询速度；

（3）模型优化：对LLM语义分析模型进行轻量化微调，减少推理时间，提升检测响应速度。

四、关键技术规格（Key Technical Specifications）

本章节明确LLM Guard项目的核心技术指标，用于指导开发与测试，确保项目落地后满足设计目标，所有指标需通过测试验证后方可交付。

指标类别

指标项

规格要求

备注

检测性能

提示词病毒检测准确率

≥98%

含Prompt注入、ASCII走私等类型



Skill病毒检测准确率

≥98.5%

覆盖ClawHub类恶意Skill



MCP病毒检测准确率

≥99%

含配置篡改、数据投毒



LLM自身病毒检测准确率

≥97%

含训练数据投毒、模型后门



整体误报率

≤0.5%

所有病毒类型平均误报率



单条检测响应时间

≤100ms

提示词/Skill/MCP/LLM病毒平均

防护性能

防护响应时间

≤100ms

检测到病毒至拦截完成



拦截成功率

≥99.5%

高危/中危病毒



性能损耗

≤10%

不影响LLM、AI Agent正常运行

修复性能

自动修复响应时间

≤3s

单条病毒修复



手动修复响应时间

≤1s

用户触发后



修复成功率

≥99%

修复后无恶意残留

更新性能

增量更新带宽

≤10MB/次

特征库/模型增量更新



全量更新带宽

≤100MB/次

特征库/模型全量更新



更新成功率

≥99.8%

含自动/手动更新



更新性能损耗

≤5%

后台更新时

系统资源

CPU占用（空闲）

≤5%

个人端/企业端通用



CPU占用（扫描）

≤30%

全量深度扫描时



内存占用（空闲）

≤100MB

个人端；企业端≤200MB



内存占用（扫描）

≤500MB

全量深度扫描时

部署适配

终端适配

Windows 10+/macOS 10.15+/Linux Ubuntu 20.04+

个人端



集群适配

支持Kubernetes分布式部署

企业级LLM集群场景

五、运行环境要求（Runtime Environment Requirements）

本章节明确LLM Guard的硬件、软件运行环境要求，分为个人端、企业端、嵌入式场景，确保开发团队部署时适配对应环境，保障软件正常运行。

5.1 个人端运行环境（终端客户端）

5.1.1 硬件要求

- CPU：x86/x64架构，主频≥2.0GHz，核心数≥2；

- 内存：≥4GB（推荐8GB）；

- 磁盘空间：≥1GB（安装包+特征库+日志，推荐2GB）；

- 网络：支持HTTP/HTTPS，带宽≥1Mbps（用于威胁情报更新）；

- 其他：支持基础图形显示（用于客户端界面渲染）。

5.1.2 软件要求

- 操作系统：Windows 10 64位及以上、macOS 10.15及以上、Linux（Ubuntu 20.04 64位+/CentOS 8+）；

- 运行时：Python 3.10+（自动安装，用于核心检测逻辑）、.NET Framework 4.8+（Windows端，用于客户端渲染）；

- 依赖软件：Firejail（Linux端，轻量级沙箱，自动安装）；

- 权限：管理员/root权限（用于拦截系统命令、修改配置、扫描系统文件）。

5.2 企业端运行环境（Web端+分布式部署）

5.2.1 硬件要求（单节点）

- CPU：x86/x64架构，主频≥2.5GHz，核心数≥8（推荐16核）；

- 内存：≥16GB（推荐32GB）；

- 磁盘空间：≥10GB（安装包+特征库+日志+数据库，推荐20GB）；

- 网络：支持HTTP/HTTPS，带宽≥10Mbps（用于威胁情报更新、节点通信）；

- 其他：支持Docker容器化部署（推荐）。

5.2.2 软件要求

- 操作系统：Linux（Ubuntu 20.04 64位+/CentOS 8+），推荐Ubuntu；

- 运行时：Python 3.10+、Node.js 16+、Docker 20.10+、Docker Compose 2.0+；

- 数据库：MySQL 8.0+、Redis 6.2+；

- Web服务器：Nginx 1.20+（用于Web端部署）；

- 依赖软件：Kubernetes（可选，用于分布式部署）、Git（用于版本管理）；

- 权限：root权限（用于部署、配置、节点管控）；

- LLM API：需接入Gemini Pro/GPT-4 API（用于语义分析模型调用，需配置有效API密钥）。

5.3 嵌入式场景运行环境（裁剪式部署）

- 硬件：CPU主频≥1.5GHz，内存≥2GB，磁盘空间≥500MB；

- 操作系统：Linux嵌入式系统（如Ubuntu Core、Buildroot）；

- 功能：仅保留核心检测、实时防护功能，裁剪Web端、日志分析、分布式管控功能；

- 依赖：Python 3.10+（轻量化版本）、Firejail（轻量级沙箱）。

六、模块交互与接口规范（Module Interaction & Interface Specifications）

明确各功能模块之间的交互逻辑、接口定义，确保开发过程中模块间协同工作，接口统一、可扩展，便于后续功能迭代与维护。
